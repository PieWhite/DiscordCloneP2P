[workspace]
resolver = "2"
members = ["crates/proto", "crates/net", "crates/app"]

[workspace.package]
version = "0.1.0"
edition = "2021"
rust-version = "1.97"
license = "MIT"

[workspace.dependencies]
fitcom-proto = { path = "crates/proto" }
fitcom-net = { path = "crates/net" }

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1

# Media-paden hebben ook in debug builds bruikbare performance nodig,
# anders is handmatig testen van screenshare/voice zinloos.
[profile.dev.package."*"]
opt-level = 2
